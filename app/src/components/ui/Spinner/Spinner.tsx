// src/components/ui/Spinner/Spinner.tsx
import { Box } from "@radix-ui/themes";
import { sSpinner } from "./Spinner.css";

const Spinner = () => {
  return (
    <Box className={sSpinner} />
  );
}

export default Spinner;
