// src/state/latestAiResponseIndexState.tsx
import { atom } from "jotai";

// JS生成用
export const LatestAiResponseIndexState = atom<number | null>(null);
// データ生成用
export const DataGenLatestAiResponseIndexState = atom<number | null>(null);
